name: Update third-party notices

on:
  pull_request:
    branches: [ "master" ]
    paths:
      - "vcpkg.json"
      - "cmake/generate_third_party_notices.cmake"
      - ".github/workflows/third_party_notices.yml"
  push:
    branches: [ "master" ]
    paths:
      - "vcpkg.json"
      - "cmake/generate_third_party_notices.cmake"
      - ".github/workflows/third_party_notices.yml"

permissions:
  contents: write
  pull-requests: write

jobs:
  update-third-party-notices:
    runs-on: windows-latest

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - uses: actions/checkout@v4
        if: github.event_name == 'pull_request'
        with:
          ref: ${{ github.head_ref }}

      - uses: actions/checkout@v4
        if: github.event_name != 'pull_request'

      - name: Set up MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          cache: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-python
            mingw-w64-x86_64-pybind11
            mingw-w64-x86_64-jq
            git

      - name: Install rang header
        run: |
          curl -L https://raw.githubusercontent.com/agauniyal/rang/master/include/rang.hpp \
            -o /mingw64/include/rang.hpp

      - name: Setup Vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: '522253caf47268c1724f486a035e927a42a90092'

      - name: Configure
        run: |
          cmake -S . -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER=gcc \
            -DCMAKE_CXX_COMPILER=g++ \
            -DVCPKG_TARGET_TRIPLET=x64-mingw-static \
            -DCMAKE_TOOLCHAIN_FILE=$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake

      - name: Generate third-party notices
        run: |
          cmake \
          -DVCPKG_INSTALLED_DIR="build/vcpkg_installed" \
          -DOUTPUT_FILE="THIRD_PARTY_NOTICES.txt" \
          -P cmake/generate_third_party_notices.cmake

      - name: Push straight to the PR branch
        if: github.event_name == 'pull_request'
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          if git diff --quiet -- THIRD_PARTY_NOTICES.txt; then
            echo "THIRD_PARTY_NOTICES.txt is already up to date."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add THIRD_PARTY_NOTICES.txt
          git commit -m "Update third-party notices"
          git push origin "HEAD:$HEAD_REF"

      - name: Open a PR for the master branch
        if: github.event_name != 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if git diff --quiet -- THIRD_PARTY_NOTICES.txt; then
            echo "THIRD_PARTY_NOTICES.txt is already up to date."
            exit 0
          fi

          branch="update-third-party-notices"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B "$branch"
          git add THIRD_PARTY_NOTICES.txt
          git commit -m "Update third-party notices"
          git push --force origin "$branch"

          existing=$(curl -sS \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/pulls?head=${GITHUB_REPOSITORY%%/*}:${branch}&base=master&state=open" \
            | jq 'length')

          if [ "$existing" -gt 0 ]; then
            echo "Open PR already exists for $branch; pushed update, no new PR needed."
            exit 0
          fi

          curl -sS -X POST \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/pulls" \
            -d @- <<EOF
          {
            "title": "Update third-party notices",
            "head": "${branch}",
            "base": "master",
            "body": "Automated update of THIRD_PARTY_NOTICES.txt triggered by changes to vcpkg.json or cmake/generate_third_party_notices.cmake."
          }
          EOF